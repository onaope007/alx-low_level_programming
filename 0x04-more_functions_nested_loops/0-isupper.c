#include "main.h"
/**
 * _isupper- returns a value of 1 if a variable is upper case
 * @c: the parameter of the given function
 * Return: 1 if true and 0 if false
 */
int _isupper(int c)
{
if (c >= 'A' && c <= 'Z')
{
return (1);
}
else
{
return (0);
}
}
