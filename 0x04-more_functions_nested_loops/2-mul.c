#include "main.h"
/**
 * mul - The function that carrioes out the task
 * @a: The first parameter
 * @b: The second parameter
 *Return: returns the product of and b
 */
int mul(int a, int b)
{
return (a * b);
}
