#include "main.h"
/**
 * _isdigit - the main function that checks the paameter
 * @c: the parameter that is to be checked
 * Return: program returns 1 if true and 0 if false
 */
int _isdigit(int c)
{
if (c >= '0' && c <= '9')
{
return (1);
}
else
{
return (0);
}
}
